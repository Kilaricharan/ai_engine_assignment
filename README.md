# ai_engine_assignment
A simple workflow engine built for the AI Engineering assignment. It supports node-based execution, shared state updates, branching, looping, and includes an example Code Review Agent that analyzes code and generates suggestions.
